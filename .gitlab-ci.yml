# Definición de etapas
stages:
  - test

# Pruebas de Cypress
cypress-test-job:
  stage: test
  image: cypress/included:12.0.0
  script:
    - echo "Ejecutando pruebas de cypress..."
    - npx cypress run

  artifacts:
    paths:
      - cypress/
    when: always

